const VideoSource = require("./VideoSource");
const FileStreamSource = require("./FileStreamSource");

// package all the VideoSource classes together
module.exports = { VideoSource, FileStreamSource };
